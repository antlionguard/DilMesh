{
  "name": "dilmesh",
  "private": true,
  "version": "0.3.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc && vite build && electron-builder",
    "build:win": "rm -rf dist-electron && vue-tsc && vite build && electron-builder --win --publish never",
    "build:mac": "rm -rf dist-electron && vue-tsc && vite build && electron-builder --mac --publish never",
    "build:all": "rm -rf dist-electron && vue-tsc && vite build && electron-builder --win --mac --publish never",
    "preview": "vite preview"
  },
  "dependencies": {
    "@aws-sdk/client-transcribe-streaming": "^3.985.0",
    "@aws-sdk/util-utf8-node": "^3.259.0",
    "@google-cloud/speech": "^7.2.1",
    "@google-cloud/translate": "^9.3.0",
    "@iconify/vue": "^5.0.0",
    "electron-store": "^11.0.2",
    "google-auth-library": "^10.5.0",
    "pinia": "^3.0.4",
    "vue": "^3.4.21",
    "vue-router": "^5.0.2"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.4",
    "autoprefixer": "^10.4.24",
    "electron": "^30.0.1",
    "electron-builder": "^24.13.3",
    "postcss": "^8.5.6",
    "tailwindcss": "^3.4.19",
    "typescript": "^5.2.2",
    "vite": "^5.1.6",
    "vite-plugin-electron": "^0.28.6",
    "vite-plugin-electron-renderer": "^0.14.5",
    "vue-tsc": "^2.0.26"
  },
  "main": "dist-electron/main.js"
}